<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <config>
    <!-- 同一ソースへの最大同時 HTTP リクエスト数を制限し、ネットワーク不安定環境でのタイムアウトを抑制する (Rancher Desktop 等の環境向け) -->
    <add key="maxHttpRequestsPerSource" value="16" />
  </config>
</configuration>
